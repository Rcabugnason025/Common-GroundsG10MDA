import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFFFFFFF);
  static const navbar = Color(0xFF6AD3FC);
  static const icon = Color(0xFF000000);
  static const textField = Color(0xFF939393);
  static const textDark = Color(0xFF333333);
  static const text = Color(0xFF000000);

  // Added missing colors
  static const primary = Color(0xFF6AD3FC); // Matching navbar
  static const secondary = Color(0xFF0288D1); // Darker blue for contrast
  static const textPrimary = Color(0xFF000000); // Matching text


}